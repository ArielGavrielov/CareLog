{
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "start": "run-p client doctor server",
    "client": "cd client; expo start; cd ..",
    "doctor": "cd doctor; yarn start; cd ..",
    "server": "cd server; npm start; cd .."
  }
}
